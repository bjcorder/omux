# cargo-audit configuration.
#
# Reserved for advisory-ignore entries when we have a documented
# reason. Empty for now; the file exists so cargo-audit can be wired
# into CI without a config-not-found stumble.
#
# Example future entry (commented):
#
#   [advisories]
#   ignore = [
#     # RUSTSEC-2024-0001: only triggered by a feature we don't enable
#     # on `some-crate`. Re-evaluate when we bump to >=1.5.
#     # "RUSTSEC-2024-0001",
#   ]
